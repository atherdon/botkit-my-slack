'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = token;

var _request = require('request');

var _request2 = _interopRequireDefault(_request);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function token(code, callback) {

  var url = 'https://github.com/login/oauth/access_token';
  var client_id = process.env.GITHUB_CLIENT_ID;
  var client_secret = process.env.GITHUB_CLIENT_SECRET;
  var json = true;
  var headers = { 'Accept': 'application/json' };
  var form = { client_id: client_id, client_secret: client_secret, code: code };
  var query = { url: url, headers: headers, form: form, json: json };

  _request2.default.post(query, function (err, res) {
    if (err) {
      callback(err);
    } else if (res.body.error) {
      callback(Error(res.body.error));
    } else {
      callback(null, res.body);
    }
  });
}